import { Page } from 'framework7-react'
import React from 'react'
import PropType from 'prop-types'

Groups.propTypes = {
	f7route: PropType.object.isRequired
}
export default function Groups({ f7route }) {
	return <Page className="groups-page">
        
    </Page>
}
