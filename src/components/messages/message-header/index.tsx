import { Flex } from 'antd'
import { memo } from 'react'

const MessageHeader = memo(() => {
	return (
		<Flex className="min-h-[80px] bg-background px-3" align="center">
			11
		</Flex>
	)
})

export default MessageHeader
